<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weimi.formx.common.mapper.FormItemMapper">

    <!-- 获取表单项列表 -->
    <select id="getList" resultType="FormItem">
        SELECT * FROM t_form_item WHERE form_id=#{formId}
    </select>

</mapper>