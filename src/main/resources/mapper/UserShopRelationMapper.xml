<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weimi.formx.common.mapper.UserShopRelationMapper">

    <!-- 获取用户店铺关系列表 -->
    <select id="getList" resultType="UserShopRelation">
        SELECT * FROM t_user_shop_relation WHERE user_id=#{userId}
    </select>

</mapper>